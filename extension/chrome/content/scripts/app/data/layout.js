/*!
 * Class: Layout Data
 * Copyright 2013 Fupslot
 * Released under the MIT license
 */

define(
[
	"app/helper", 
	"data/layout.json"
],

function(helper, _data) {
	var data = Object.create(_data);
	// =========================
	// = VALIDATE PAGE OBJECTS =
	// =========================
	if (helper.isArray(_data.pages)) {
// 
	}
	// =========================

});